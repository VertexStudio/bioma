[project]
name = "bioma"
version = "0.1.0"
description = "A Multi-Agent Cognition Engine"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.0",
    "torch>=2.5.1",
    "transformers>=4.46.3",
]
